# 🔄 CẬP NHẬT: DROPDOWN HỌA LAN ĐỘNG

## ✅ Những Gì Đã Thay Đổi

### 1. **HTML (index.html)**
**Trước:**
```html
<select class="form-control" name="orchid-type" id="orchid-type" required="">
  <option value="">-- Chọn loại hoa lan --</option>
  <option value="phalaenopsis">Lan Phalaenopsis</option>
  <option value="cattleya">Lan Cattleya</option>
  <option value="dendrobium">Lan Dendrobium</option>
  <option value="oncidium">Lan Oncidium</option>
</select>
```

**Sau:**
```html
<select class="form-control" name="orchid-type" id="orchid-type" required="">
  <option value="">-- Chọn loại hoa lan --</option>
  <!-- Options will be generated by JavaScript -->
</select>
```

### 2. **JavaScript (assets/js/products.js)**
✅ Thêm function `populateOrchidTypeDropdown()` - tự động load categories từ JSON  
✅ Thêm helper functions:
   - `getProductsByCategory()` - lấy danh sách sản phẩm theo danh mục
   - `getCategoryById()` - lấy thông tin danh mục

---

## 🎯 Cách Hoạt Động

1. Trang load → `loadProductsData()` gọi `fetch('assets/data/products.json')`
2. JSON loaded → `renderProductMenu()` & `populateOrchidTypeDropdown()` chạy
3. Dropdown tự động populate từ `productsData.categories`
4. Người dùng chọn danh mục → value = category ID (ví dụ: "phalaenopsis")

---

## 📋 Cấu Trúc products.json

```json
{
  "categories": [
    {
      "id": "phalaenopsis",        ← Value gửi tới server
      "name": "Lan Phalaenopsis",  ← Text hiển thị trong dropdown
      "tab": "menu-starters",
      "products": [...]
    }
  ]
}
```

---

## 🔧 Để Thêm Danh MỤC MỚI

**B1:** Sửa `assets/data/products.json`

```json
{
  "id": "vanda",
  "name": "Lan Vanda",
  "tab": "menu-vanda",
  "products": [...]
}
```

**B2:** Reload trang → Dropdown tự update

---

## 📤 Xử Lý Form

Khi form submit, dropdown gửi:
```
name="orchid-type"
value="phalaenopsis" (hoặc ID danh mục khác)
```

**Để lấy chi tiết sản phẩm:**
```javascript
const categoryId = document.getElementById('orchid-type').value;
const products = getProductsByCategory(categoryId);
```

---

## 🎁 Bonus Functions

### Get products by category
```javascript
const products = getProductsByCategory('phalaenopsis');
// Returns: [
//   { id: 1, name: "...", price: "399.000đ", ... },
//   { id: 2, name: "...", price: "399.000đ", ... },
//   ...
// ]
```

### Get category info
```javascript
const category = getCategoryById('phalaenopsis');
// Returns: { id: "phalaenopsis", name: "Lan Phalaenopsis", tab: "menu-starters", products: [...] }
```

---

## ✨ Lợi Ích

| Trước | Sau |
|-------|-----|
| Hardcode options | Load từ JSON ✓ |
| Thêm danh mục = sửa HTML | Thêm danh mục = sửa JSON ✓ |
| Dữ liệu menu ≠ dropdown | Dữ liệu đồng bộ 100% ✓ |
| Không thể export | Sẵn sàng cho Excel/CSV import ✓ |

---

## 🚀 Bước Tiếp Theo

1. **Kiểm tra:** Reload trang, xem dropdown có danh mục không
2. **Sửa JSON:** Thêm/xoá danh mục, dropdown tự update
3. **Nâng cao:** Thêm option cho sản phẩm cụ thể (nếu cần)

---

**Ngày cập nhật:** 2025-02-10
